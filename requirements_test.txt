pytest-homeassistant-custom-component==0.13.348
feedparser>=6.0.11
# the documented coverage command and the CI coverage gate need it explicitly;
# it otherwise only resolves as a transitive dependency
pytest-cov>=6.0.0
ruff==0.16.0
